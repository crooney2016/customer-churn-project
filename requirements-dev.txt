# Development and Testing Dependencies
# Install with: pip install -r requirements-dev.txt
# These are not needed for production Azure Function deployment

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0

# Code Quality
ruff>=0.1.0
codehealthanalyzer>=1.0.0  # Code quality scoring (0-100) similar to CodeClimate/SonarQube

# Type Checking (already in use)
# Pyright is typically installed via VS Code extension or npm
# pylint>=2.17.0  # Optional - keep for deep analysis
